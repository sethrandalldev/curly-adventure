import { TextField } from "@material-ui/core";
import React from "react";

const PageSearch = () => {
  return <TextField id="page-search" placeholder="Search" variant="outlined" />;
};

export default PageSearch;
